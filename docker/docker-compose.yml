services:
  leanspec:
    image: ghcr.io/codervisor/leanspec:latest
    ports:
      - "3000:3000"
    volumes:
      - leanspec-data:/root/.lean-spec
    environment:
      - PORT=3000

volumes:
  leanspec-data:
